import type {
  Tool,
  ToolContext,
  ToolResult,
  ToolTag,
  ToolFewShot,
} from "./types";
import { defineTool } from "./define";

export { defineTool };
export type { Tool, ToolContext, ToolResult, ToolTag, ToolFewShot };
